extends Resource
class_name Bullet_Resource

export (SpriteFrames) var frames
export (float, 100, 1200) var move_rate
export (int, 1, 100) var damage
export (float, 0.05, 1.0) var trauma

export (float, -50.0, 50.0) var volume
export (int) var clip
